version: "3.8"

services:
  chat-buddy-dev:
    build:
      context: .
      dockerfile: docker/Dockerfile.dev
    container_name: chat-buddy-dev
    hostname: chat-buddy-host
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GOOGLE_OAUTH_CLIENT_ID=${GOOGLE_OAUTH_CLIENT_ID:-}
      - GOOGLE_OAUTH_CLIENT_SECRET=${GOOGLE_OAUTH_CLIENT_SECRET:-}
      - CHAT_BUDDY_RESPONSE_DEBOUNCE_MS=${CHAT_BUDDY_RESPONSE_DEBOUNCE_MS:-2200}
      - CHAT_BUDDY_PENDING_REPLY_TTL_HOURS=${CHAT_BUDDY_PENDING_REPLY_TTL_HOURS:-1}
      - CHAT_BUDDY_PENDING_REPLY_CLEANUP_INTERVAL_MS=${CHAT_BUDDY_PENDING_REPLY_CLEANUP_INTERVAL_MS:-300000}
      - NODE_ENV=development
    volumes:
      - .:/app
      - /app/node_modules
      - chat-buddy-data:/root/.botwithaki
    command: npm run dev
    restart: unless-stopped
    stdin_open: true
    tty: true

volumes:
  chat-buddy-data:
